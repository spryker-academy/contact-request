{% extends template('page-layout-main') %}

{% define data = {
    contactRequest: _view.contactRequest
} %}

{% block content %}
    {% if data.contactRequest is not null %}
        The contact request message is: {{ data.contactRequest.message }}
    {% else %}
        No contact request found :(
    {% endif %}
{% endblock %}
